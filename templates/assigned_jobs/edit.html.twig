{% extends 'base1.html.twig' %}

{% block body %}
    <main>
        <div class="container">
            <style>
                .content-box {
                   margin-left: auto;
                    margin-right: auto;
                    max-width: 600px; /* Adjust the max-width as needed */
                    margin-top: 50px; /* Adjust the top margin as needed */
                    padding: 70px; 
                }
            </style>

            <div class="content-box">
                <h1>Edit AssignedJobs</h1>

                {{ include('assigned_jobs/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_assigned_jobs_index') }}">Back to list</a>

                {{ include('assigned_jobs/_delete_form.html.twig') }}
            </div>
        </div>
    </main>
{% endblock %}
