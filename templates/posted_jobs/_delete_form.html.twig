<form method="post" action="{{ path('app_posted_jobs_delete', {'id': posted_job.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ posted_job.id) }}">
    <button class="btn"><i class="ri ri-close-circle-fill text-danger"></i></button>
</form>
